# TeamKey 管理后台 UI 设计说明

## 一、设计目标

* **清晰**：管理后台核心是配置与审计，界面需一目了然。
* **安全感**：采用沉稳的配色，重要操作有二次确认。
* **高效**：表格化、卡片化展示，快速定位问题与配置项。

---

## 二、界面风格

1. **配色方案**

   * 主色：深蓝 (#1A237E) 或科技紫 (#4527A0)
   * 辅助色：绿色 (#43A047) 用于成功，红色 (#E53935) 用于警告/删除
   * 背景：浅灰 (#F5F7FA)，卡片/表格白底
   * 提供暗色模式（背景 #1E1E1E，主色适当提亮）

2. **排版与字体**

   * 标题（18px Bold）、副标题（16px Medium）、正文（14px Regular）
   * 数据表格字体小一号（13px），保证可读性

3. **组件风格**

   * 表格（带筛选/分页/导出）
   * 卡片（团队信息、用户信息）
   * 弹窗（新增配置/二次确认）
   * 左侧导航 + 顶部状态栏布局

---

## 三、信息架构（主要模块）

### 1. 登录 & 安装

* 登录页：管理员账号 + 密码输入框
* 首次安装向导：分步表单（站点 URL → 数据库配置 → Redis → 小程序 AppID/Secret → 管理员账号）
* 成功后跳转到控制台首页

### 2. 控制台首页

* 概览卡片：

  * 用户总数、团队数、账号数
  * 最近 24 小时操作日志数
  * Redis / 数据库状态指示（绿灯/红灯）
* 快捷入口：系统配置 / 日志导出 / 团队管理

### 3. 用户管理

* 用户表格：ID、昵称、OpenID、所属团队数、最近登录时间
* 支持搜索、按时间排序
* 可点击用户查看详情（所属团队列表、权限摘要）

### 4. 团队管理

* 团队列表：ID、团队名、创建人、成员数、账号数
* 点击进入团队详情：

  * 成员表格（头像、昵称、角色、加入时间）
  * 账号表格（服务名、账号标识、创建时间）

### 5. 日志与审计

* 表格视图：时间、用户、团队、操作类型、目标对象、IP
* 筛选：按用户/团队/操作类型/时间范围
* 导出按钮：生成 CSV/JSON 文件 → 对象存储 → 提供下载链接
* 时间轴视图（可选）：更直观展示关键操作

### 6. 系统配置

* 站点 URL
* 数据库配置（只读展示，修改需重启）
* Redis 配置（连接状态）
* 对象存储配置
* 小程序 AppID/Secret（加密存储，部分遮挡显示）
* 安全策略开关（是否允许密包分享、日志保存时长）

---

## 四、交互说明

* **危险操作（删除团队/账号/用户）**

  * 弹窗二次确认 + 输入关键字确认
* **配置修改**

  * 表单修改后统一「保存」按钮，保存前需校验
* **数据导出**

  * 点击导出 → Loading 动画 → 完成后生成下载链接 → Toast 提示「导出成功，有效期 24 小时」

---

## 五、未来扩展

* **监控模块**：接入 Prometheus + Grafana，展示 API 请求数、错误率、延迟
* **多租户支持**：管理后台切换租户视角
* **操作回放**：关键权限操作支持审计回放（日志 + 关键截图）

---

📌 **总结**

* 管理后台 UI 以 **左侧导航 + 右侧工作区** 为主，突出「配置与审计」功能。
* 风格偏沉稳，保证安全感与专业感。
* 与小程序端形成互补：小程序注重 **使用体验**，后台注重 **安全管控与可视化**。
