# TeamKey UI 设计说明

## 一、设计目标

* **简洁**：突出安全与团队协作，去掉冗余装饰。
* **现代**：浅色为主，深色模式可选，配合品牌主色（蓝/紫/青）。
* **易用**：信息层级清晰，常用操作 ≤2 步可达。
* **一致性**：符合微信小程序 UI 习惯，减少学习成本。

---

## 二、界面风格

1. **配色方案**

   * 主色：安全感强的 **蓝色 (#2979FF)** 或 **青色 (#00BFA5)**。
   * 辅助色：强调交互的 **绿色 (#4CAF50)** 和 **橙色 (#FF9800)**。
   * 背景色：浅灰/白 (#F9FAFB)，深色模式时使用 #1E1E1E。
   * 警告色：红色 (#F44336)，用于权限拒绝、错误提示。

2. **字体与排版**

   * 字体：默认微信小程序系统字体（苹方 / 思源黑体）。
   * 标题字号：18–20 px，正文：14–16 px。
   * 字重：标题 **Bold**，正文 **Regular**。

3. **图标与视觉元素**

   * 图标库：采用 **线性图标（Feather/IconPark）**，风格轻量。
   * 动态码采用「数字安全键盘」风格卡片。
   * 团队/成员使用头像圆形边框，增强识别度。

---

## 三、信息架构（主要页面）

1. **首页（账号列表）**

   * 顶部：搜索框、团队切换。
   * 中部：账号卡片（显示账号名、服务图标、动态码）。
   * 底部导航：

     * 【首页】账号列表
     * 【团队】成员与权限
     * 【日志】操作记录
     * 【我的】个人设置

2. **账号详情页**

   * 动态码大卡片（实时刷新，带倒计时进度条）。
   * 账号信息（名称、绑定服务、权限）。
   * 操作按钮：分享 / 删除 / 权限管理。

3. **团队管理页**

   * 团队信息卡：名称、成员数。
   * 成员列表：头像 + 名称 + 角色标签。
   * 邀请按钮（微信联系人/手机号）。
   * 权限修改（下拉选择：管理员/成员/访客）。

4. **日志审计页**

   * 时间轴样式，记录「谁在什么时候做了什么操作」。
   * 筛选条件：按团队、账号、用户、操作类型。

5. **个人设置页**

   * 我的信息：头像、昵称、OpenID（隐藏部分）。
   * 安全设置：PIN / 云备份 / 恢复码导出。
   * 帮助 & 关于。

---

## 四、交互说明

1. **动态码展示**

   * 每 30 秒自动刷新，带圆环/进度条动画。
   * 支持「一键复制」。

2. **分享流程**

   * 入口：账号详情 → 分享按钮。
   * 弹窗：选择分享方式（一次性链接/团队成员）。
   * 成功后提示「有效期 5 分钟，一次性领取」。

3. **权限操作**

   * 管理员长按成员卡片 → 修改权限。
   * 成员进入账号详情 → 仅能查看动态码（若权限为 view）。

4. **错误与反馈**

   * 网络错误：Toast 提示「请检查网络」。
   * 权限不足：红色 Banner 提示「你没有权限访问此账号」。
   * 成功操作：绿色 Toast「已成功」。

---

## 五、未来扩展（UI 保留空间）

* **多主题**：亮色/暗色自动适配。
* **多账号分组**：折叠式卡片。
* **二维码导出/导入**：迁移账号时展示为大二维码。

---

📌 **总结**：

* 整体风格 → **轻量、卡片化、安全感强**。
* 主要功能入口在底部导航，核心场景（动态码查看/分享/权限管理）**≤2 步到达**。
* 动态码界面动画与色彩强调时间敏感性，团队管理界面强调成员角色和操作透明性。
